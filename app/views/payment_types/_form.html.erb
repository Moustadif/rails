<%= form_with(model: payment_type, local: true) do |form| %>
  <% if payment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_type.errors.count, "error") %> prohibited this payment_type from being saved:</h2>

      <ul>
      <% payment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :payment_via %>
    <%= form.select :payment_via, PaymentType::PAYMENT_TYPES %>
  </div>

  <div class="field">
    <%= form.hidden_field :cart_id, value: @current_cart.id %>
  </div>

  <div class="actions">
    <%= form.submit 'Choose Payment Type' %>
  </div>
<% end %>
