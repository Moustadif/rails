<%= form_with(model: delivery_type, local: true) do |form| %>
  <% if delivery_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_type.errors.count, "error") %> prohibited this delivery_type from being saved:</h2>

      <ul>
      <% delivery_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :delivery_via %>
    <%= form.select :delivery_via, DeliveryType::DELIVERY_TYPES %>
  </div>

  <div class="field">
    <%= form.hidden_field :cart_id, value: @current_cart.id %>
  </div>

  <div class="actions">
    <%= form.submit 'Choose Delivery Type' %>
  </div>
<% end %>
